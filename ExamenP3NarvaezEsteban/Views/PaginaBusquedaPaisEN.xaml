<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ExamenP3NarvaezEsteban.ViewModels"
             x:Class="ExamenP3NarvaezEsteban.Views.PaginaBusquedaPaisEN">
    <ContentPage.BindingContext>
        <local:PaginaBusquedaPaisViewModelEN />
    </ContentPage.BindingContext>
    <StackLayout>
        <Entry x:Name="nombrePaisEN" Text="{Binding NombrePais}" Placeholder="Ingrese nombre del país" />
        <Button x:Name="botonBuscarEN" Text="Buscar" Command="{Binding BuscarPaisCommand}" />
        <Button x:Name="botonLimpiarEN" Text="Limpiar" Command="{Binding LimpiarCommand}" />
        <Label Text="{Binding MensajeResultado}" />
    </StackLayout>
</ContentPage>
